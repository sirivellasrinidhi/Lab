git init
git config --global user.name"sirivellasrinidhi"
email id
git clone repository url
cd repository name
touch README.md
4 lines from git, remove 2nd line and 1>
git push origin main




Git Basic Commands and Their Functions:

1. Check Git Version:
   git --version
   - Shows the installed Git version.

2. Initialize a New Git Repository:
   git init
   - Initializes a new Git repository in the current folder.

3. Clone a Repository:
   git clone <repository-url>
   - Clones an existing repository to your local machine.

4. Check the Status:
   git status
   - Displays the status of the files in your repository (modified, added, etc.).

5. Track New Files:
   git add <file-name>
   - Adds a specific file to the staging area.

   git add .
   - Adds all modified files in the repository to the staging area.

6. Commit Changes:
   git commit -m "Your commit message"
   git commit -m "first image"
   - Commits the staged changes with a descriptive message.

7. View Commit History:
   git log
   - Displays the commit history with details like commit ID, author, and message.

8. Create a New Branch:
   git checkout -b <branch-name>
   - Creates and switches to a new branch.

9. Switch Branches:
   git checkout <branch-name>
   - Switches to an existing branch.

10. Merge Branches:
    git merge <branch-name>
    - Merges changes from the specified branch into the current branch.

11. Push Changes to Remote:
    git push origin <branch-name>
    - Pushes local commits to the specified branch on the remote repository.

12. Pull Changes from Remote:
    git pull origin <branch-name>
    - Fetches and merges changes from the remote repository to your local branch.

13. View Remote Repositories:
    git remote -v
    - Displays the remote repositories linked to the local repository.

14. Delete a Branch Locally:
    git branch -d <branch-name>
    - Deletes a local branch.

15. Delete a Remote Branch:
    git push origin --delete <branch-name>
    - Deletes a branch from the remote repository.


GitHub and Remote Repositories:

1. Create a GitHub Account:
   - Go to https://github.com/ and sign up for a free account.

2. Create a New Repository on GitHub:
   - Click the "New" button in GitHub to create a new repository.

3. Push Local Code to GitHub:
   git remote add origin <repository-url>
   - Links your local repository to the GitHub repository.

   git push -u origin master
   - Pushes local commits to the GitHub repository.



